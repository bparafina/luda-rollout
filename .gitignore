# Beads issue tracker — contains assignee emails and names; never commit
.beads/

# Build artifacts
dist/
kubectl-rollout

# asciinema raw recordings (large, regenerate with: asciinema rec)
demo/*.cast

# Media assets — copyrighted content, must be sourced separately (see README)
internal/audio/rollout.mp3
internal/gif/rollout.gif
media/rollout.mp3
media/rollout.gif
